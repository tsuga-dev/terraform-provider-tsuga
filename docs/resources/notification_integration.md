---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "tsuga_notification_integration Resource - tsuga"
subcategory: ""
description: |-
  Notification destinations (PagerDuty, webhook, Jira, etc.) used as targets by notification rules. Slack is not supported here because it is set up through an OAuth install flow in the Tsuga app.
---

# tsuga_notification_integration (Resource)

Notification destinations (PagerDuty, webhook, Jira, etc.) used as targets by notification rules. Slack is not supported here because it is set up through an OAuth install flow in the Tsuga app.

## Example Usage

```terraform
# Example: PagerDuty integration
# Secrets are write-only: they are sent to Tsuga but never stored in the plan or state.
# Bump `secrets_version` after rotating a secret so Terraform sends the new value.
resource "tsuga_notification_integration" "pagerduty" {
  name            = "primary-on-call"
  secrets_version = "1"

  setting = {
    pagerduty = {
      integration_key = "abcdef1234567890abcdef1234567890"
    }
  }
}

# Example: Webhook integration with bearer auth and a JSON payload template
resource "tsuga_notification_integration" "webhook" {
  name            = "internal-webhook"
  secrets_version = "1"

  setting = {
    webhook = {
      url    = "https://example.com/hooks/tsuga"
      method = "POST"

      authentication = {
        bearer = { token = "shhh" }
      }

      payload_template = {
        type = "json"
        value = jsonencode({
          title  = "{{ alert_title }}"
          status = "{{ alert_status }}"
        })
      }
    }
  }

  tags = [
    {
      key   = "team"
      value = "platform"
    },
  ]
}

# Example: Jira integration
resource "tsuga_notification_integration" "jira" {
  name            = "jira-ops"
  secrets_version = "1"

  setting = {
    jira = {
      site_url  = "https://your-company.atlassian.net"
      email     = "ops@your-company.com"
      api_token = "your-atlassian-api-token"
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name of the integration
- `setting` (Attributes) Destination configuration. Exactly one of google_chat, grafana_irm, incident_io, jira, microsoft_teams, pagerduty, servicenow, squadcast, or webhook must be set. The integration type cannot be changed after creation; changing which one is set replaces the resource. (see [below for nested schema](#nestedatt--setting))

### Optional

- `secrets_version` (String) Secrets are write-only and never stored in state, so Terraform cannot detect when one changes. Set this to any new value after rotating a secret to have it sent again.
- `tags` (Attributes List) List of key/value tags applied to the resource (see [below for nested schema](#nestedatt--tags))

### Read-Only

- `id` (String) Unique identifier of the integration

<a id="nestedatt--setting"></a>
### Nested Schema for `setting`

Optional:

- `google_chat` (Attributes) (see [below for nested schema](#nestedatt--setting--google_chat))
- `grafana_irm` (Attributes) (see [below for nested schema](#nestedatt--setting--grafana_irm))
- `incident_io` (Attributes) (see [below for nested schema](#nestedatt--setting--incident_io))
- `jira` (Attributes) (see [below for nested schema](#nestedatt--setting--jira))
- `microsoft_teams` (Attributes) (see [below for nested schema](#nestedatt--setting--microsoft_teams))
- `pagerduty` (Attributes) (see [below for nested schema](#nestedatt--setting--pagerduty))
- `servicenow` (Attributes) (see [below for nested schema](#nestedatt--setting--servicenow))
- `squadcast` (Attributes) (see [below for nested schema](#nestedatt--setting--squadcast))
- `webhook` (Attributes) (see [below for nested schema](#nestedatt--setting--webhook))

<a id="nestedatt--setting--google_chat"></a>
### Nested Schema for `setting.google_chat`

Required:

- `webhook_url` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Google Chat space webhook URL


<a id="nestedatt--setting--grafana_irm"></a>
### Nested Schema for `setting.grafana_irm`

Required:

- `webhook_url` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Grafana IRM integration webhook URL


<a id="nestedatt--setting--incident_io"></a>
### Nested Schema for `setting.incident_io`

Required:

- `alert_source_config_id` (String) incident.io alert source config ID
- `token` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) incident.io alert source token


<a id="nestedatt--setting--jira"></a>
### Nested Schema for `setting.jira`

Required:

- `api_token` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Atlassian API token minted for the account
- `email` (String) Atlassian account email used together with the API token for Basic authentication
- `site_url` (String) Base URL of the Jira Cloud site, like "https://your-company.atlassian.net"


<a id="nestedatt--setting--microsoft_teams"></a>
### Nested Schema for `setting.microsoft_teams`

Required:

- `webhook_url` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Microsoft Teams incoming webhook URL


<a id="nestedatt--setting--pagerduty"></a>
### Nested Schema for `setting.pagerduty`

Required:

- `integration_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) PagerDuty Events API v2 integration key


<a id="nestedatt--setting--servicenow"></a>
### Nested Schema for `setting.servicenow`

Required:

- `instance_name` (String) Instance name of the ServiceNow instance, i.e. the subdomain part of the instance URL, like "my-instance" in "my-instance.service-now.com"
- `password` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) ServiceNow password
- `username` (String) ServiceNow username


<a id="nestedatt--setting--squadcast"></a>
### Nested Schema for `setting.squadcast`

Required:

- `webhook_url` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Squadcast webhook URL


<a id="nestedatt--setting--webhook"></a>
### Nested Schema for `setting.webhook`

Required:

- `authentication` (Attributes) Authentication applied to the webhook request. Exactly one of bearer, basic, custom_header, or none must be set. (see [below for nested schema](#nestedatt--setting--webhook--authentication))
- `method` (String) HTTP method used to call the URL
- `payload_template` (Attributes) Body template sent to the webhook. `type` is json or form; `value` is the template body encoded as a JSON string (a JSON object for `json`, a flat object of strings/numbers/booleans/arrays for `form`). (see [below for nested schema](#nestedatt--setting--webhook--payload_template))
- `url` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Destination URL

Optional:

- `custom_headers` (Map of String) Additional custom headers sent with every request
- `event_name_mapping` (Attributes) Overrides for the event name Tsuga substitutes into the payload template (see [below for nested schema](#nestedatt--setting--webhook--event_name_mapping))

<a id="nestedatt--setting--webhook--authentication"></a>
### Nested Schema for `setting.webhook.authentication`

Optional:

- `basic` (Attributes) (see [below for nested schema](#nestedatt--setting--webhook--authentication--basic))
- `bearer` (Attributes) (see [below for nested schema](#nestedatt--setting--webhook--authentication--bearer))
- `custom_header` (Attributes) (see [below for nested schema](#nestedatt--setting--webhook--authentication--custom_header))
- `none` (Attributes) Send the request unauthenticated. Set as an empty object: `none = {}`. (see [below for nested schema](#nestedatt--setting--webhook--authentication--none))

<a id="nestedatt--setting--webhook--authentication--basic"></a>
### Nested Schema for `setting.webhook.authentication.basic`

Required:

- `password` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Basic auth password
- `username` (String) Basic auth username


<a id="nestedatt--setting--webhook--authentication--bearer"></a>
### Nested Schema for `setting.webhook.authentication.bearer`

Required:

- `token` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Bearer token


<a id="nestedatt--setting--webhook--authentication--custom_header"></a>
### Nested Schema for `setting.webhook.authentication.custom_header`

Required:

- `key` (String) Header name
- `value` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) Header value


<a id="nestedatt--setting--webhook--authentication--none"></a>
### Nested Schema for `setting.webhook.authentication.none`



<a id="nestedatt--setting--webhook--payload_template"></a>
### Nested Schema for `setting.webhook.payload_template`

Required:

- `type` (String)
- `value` (String) Template body encoded as a JSON string


<a id="nestedatt--setting--webhook--event_name_mapping"></a>
### Nested Schema for `setting.webhook.event_name_mapping`

Required:

- `alert` (String)
- `ok` (String)




<a id="nestedatt--tags"></a>
### Nested Schema for `tags`

Required:

- `key` (String)
- `value` (String)
